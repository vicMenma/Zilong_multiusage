# pyrofork>=2.3.40 added concurrent_transmissions (parallel MTProto upload slots).
# Pin to a known-good range; the bot gracefully falls back if an older build is used.
pyrofork>=2.3.40
# uvloop replaces Python's default asyncio event loop with libuv (same engine as Node.js).
# Gives 2-4x improvement on network I/O — critical for high-throughput Telegram uploads.
uvloop
TgCrypto
aiohttp
yt-dlp
aria2p
py7zr
rarfile
python-dotenv
google-api-python-client
google-auth
google-auth-httplib2
psutil
Pillow
moviepy
