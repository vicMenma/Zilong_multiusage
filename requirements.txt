pyrofork==2.2.11
# uvloop: replaces Python's default asyncio with libuv — 2-4x faster I/O for uploads.
uvloop
TgCrypto
aiohttp
yt-dlp
aria2p
py7zr
rarfile
python-dotenv
google-api-python-client
google-auth
google-auth-httplib2
psutil
Pillow
# seedrcc: Seedr.cc API wrapper — works for free + premium accounts.
# Uses the same internal endpoints as the Kodi/Chrome extensions.
# REST API v1 (Basic Auth) returns 401 for free accounts — seedrcc is required.
seedrcc>=2.0.2
# pyngrok removed — keep-alive uses Serveo (SSH tunnel, no token needed) on EC2/VPS.
