pyrofork==2.2.11
# uvloop: replaces Python's default asyncio with libuv — 2-4x faster I/O for uploads.
uvloop
TgCrypto
aiohttp
yt-dlp
aria2p
py7zr
rarfile
python-dotenv
google-api-python-client
google-auth
google-auth-httplib2
psutil
Pillow
# pyngrok removed — keep-alive uses Serveo (SSH tunnel, no token needed) on EC2/VPS.
# seedrcc REMOVED — we use Seedr REST API v1 directly via aiohttp (already installed).
# Zero extra dependencies, zero library version issues.
